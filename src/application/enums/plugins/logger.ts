export enum LoggerBaseMessages {
  DEBUG = '[LOGGER DEBUG]',
  LOG = '[LOGGER LOG]',
  WARN = '[LOGGER WARN]',
  ERROR = '[LOGGER ERROR]',
  INFO = '[LOGGER INFO]',
  FORCE = '[LOGGER FORCE]',
}

export enum LoggerLevels {
  LOG = 0,
  DEBUG = 1,
  INFO = 2,
  WARN = 3,
  ERROR = 4,
  OFF = 99
}